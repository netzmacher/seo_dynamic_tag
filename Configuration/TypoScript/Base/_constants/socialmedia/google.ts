plugin.tx_seodynamictag {
  socialmedia {
    # cat=Seo Dynamic Tag - Social media - Google//100;   type=boolean;        label= Disabled:Disable it, if you don't need meta tags for Google in the HTML head.
    google.disabled = 1
  }
}
